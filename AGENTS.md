# 3D 魔方

## 要做什么
网页上一个 3x3 魔方：
- 用户输入 n，点按钮随机打乱 n 次
- 点"一键复原"，魔方自己复原
- 有按钮可以一步一步看复原过程
- 用户可以随时自己转动魔方

## 不要做什么
不要计时器、不要排行榜、不要登录、不要 2x2、不要手机适配。
想加任何我没提过的东西，先问我。

## 规矩
1. 每次只做一小步，做完告诉我改了哪些文件
2. 每做完一步必须写测试，并且跑 npm test
3. 测试不过就停下来告诉我，不要自己接着乱改
4. 我看不懂代码，你要用大白话解释你干了什么
5. 不要动 git，存档我自己来，告诉我具体应该怎么做，为什么这么做

## 面与方向的约定（不许改）

### 六个面（标准记号）
- U = 上（Up）    D = 下（Down）
- L = 左（Left）  R = 右（Right）
- F = 前（Front，相机初始正对的那面）
- B = 后（Back）

### 方向
- 字母后面加 ' 表示逆时针，例如 R'
- 数字 2 表示转 180°，例如 R2

### 顺时针对照表（关键，别搞反）
"顺时针"的定义：站在该面的正前方，正对着它看时的顺时针方向。
- U：从上往下看是顺时针
- D：从下往上看是顺时针
- 其余四面：从该面外侧看是顺时针

### 面与面的关系
- F 的对面是 B，U 的对面是 D，L 的对面是 R

### 拖动视角
用户拖动只改变相机位置，不改变魔方的状态，
也不改变"哪面是 F/U/L/R/B"。禁止让"前面"跟着相机变。

